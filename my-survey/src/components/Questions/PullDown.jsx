import React from "react";
export default function PullDown({question,onChange,onDelete,viewOnly=false,namePrefix=''}) {
    const authChange=(up)=>{

    }
    return (
        <div>

        </div>
    )
}